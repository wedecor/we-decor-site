/**
 * @deprecated There is no parent company. Use `buildLocalBusiness`.
 * Kept as an alias so legacy imports still emit the single LocalBusiness node.
 */
export { buildLocalBusiness as buildOrganization } from './local-business';
